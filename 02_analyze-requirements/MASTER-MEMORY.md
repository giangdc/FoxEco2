# MASTER-MEMORY — Cross-Version Registry

> Cập nhật lần cuối: 2026-07-29
> Active version: v1.0

## 1. Version Registry
| Version | Release Date | Input Folder | Analyze Folder | Status | Tổng DOC | Tổng SC (all) | Tổng SC (new+mod) | Parent |
|---------|-------------|-------------|----------------|--------|----------|--------------|-------------------|--------|
| v1.0 | TBD | `00_input/v1.0/` | `02_analyze-requirements/v1.0/` | Analyzed | 4 | 92 | 92 | — (version đầu tiên) |

## 2. DOC ID Registry (Global)
| DOC ID | Version | File | Loại | Modules |
|--------|---------|------|------|---------|
| DOC-v1.0-01 | v1.0 | `FoxEco BRD/FoxEco BRD v3.1 (1).html` | HTML (BRD v3.1) | USR, ORD, ASN, DLV, GIFT, CNL, NTF, TS |
| DOC-v1.0-02 | v1.0 | `FoxEco Demo 3 vai tro (standalone)/tổng hợp từ file demo.docx` | Word (PRD tái dựng từ demo) | USR, ORD, ASN, DLV, GIFT, NTF |
| DOC-v1.0-03 | v1.0 | `FoxEco Demo 3 vai tro (standalone)/FoxEco Demo 3 vai tro (standalone) (2).html` | HTML (prototype tương tác, reference-only) | ASN, DLV |
| DOC-v1.0-04 | v1.0 | `Fox Eco Doc/images/*` (82 ảnh, FigJam board `canvas.fig`) | Figma UI design mockup (status bar "9:41" — mẫu chuẩn Apple design, KHÔNG phải screenshot chụp máy thật) — nguồn thiết kế UI text/button chi tiết nhất hiện có | USR, ORD, ASN, DLV, GIFT, CNL, NTF |

## 3. Scenario Lifecycle (Cross-Version)
> Version ĐẦU (tất cả NEW) — dùng roll-up gọn theo Module.
> **Cập nhật 2026-07-28:** bảng này bị stale từ đợt health-check 2026-07-27 (chỉ sync `MEMORY.md`/`requirement_traceability.md`, bỏ sót bảng này) — nhân tiện UPDATE BRD v3.2 đã recompute lại đúng theo `MEMORY.md §3` hiện tại.

| Module | Origin | Lifecycle | Count |
|--------|--------|-----------|-------|
| USR | v1.0 | NEW | 7 |
| ORD | v1.0 | NEW | 30 |
| ASN | v1.0 | NEW | 12 |
| DLV | v1.0 | NEW | 14 |
| GIFT | v1.0 | NEW | 7 |
| CNL | v1.0 | NEW | 5 |
| NTF | v1.0 | NEW | 9 |
| TS | v1.0 | NEW | 3 |

## 4. Regression Scope
### v1.0
**Phải test (new + modified):** toàn bộ 82 scenario NEW (version đầu tiên, chưa có regression scope).

**Nên regression (carried — high risk):** — (chưa có version trước)

**Không cần test (carried — low risk, stable):** — (chưa có version trước)

## 5. Version Comparison
(Từ v2 trở đi)

## 6. TC Files Registry
| Version | TC-MASTER File | Tổng TC | Ngày consolidate | Status |
|---------|---------------|---------|-------------------|--------|
| v1.0 | `ISC_FoxEco_v1.0_TC_v1_R1.xlsx` (alias: `TC-MASTER-v1.0.xlsx`, `TC-MASTER-LATEST.xlsx`) | 171 (TC_01 Hoạt động:20, TC_02 Cá nhân:15, TC_03 Thông báo:27, TC_04 Đăng tin:109) | 2026-07-29 | ✅ Consolidated · Thông báo Reviewed 85/100 (CONDITIONAL); Đăng tin Reviewed 85/100 (CONDITIONAL, capped — chưa re-review kể từ 2026-07-29, gồm cả đợt PRUNE 4 TC trùng lặp) |

## 7. Downstream Path Registry
| Skill | Active Version Path |
|-------|-------------------|
| generate-tc | `02_analyze-requirements/v1.0/` → `03_test-cases/v1.0/` |
| review-tc | `03_test-cases/v1.0/` |
| vibe-test | `03_test-cases/v1.0/` → `08_test-runs/vibe/` |
| log-bug | `05_bug-reports/` |
| test-report | `09_reports/` |

## 8. Pipeline Status — v1.0
> Thứ tự + tên skill khớp PIPELINE.md. Status ∈ NOT_STARTED / IN_PROGRESS / PARTIAL / COMPLETED / SKIPPED / FAILED.

| # | Skill | Status | Last Run | Scope | Output | Notes |
|---|-------|--------|----------|-------|--------|-------|
| 1 | init-project | COMPLETED | 2026-07-24 | — | CLAUDE.md/PIPELINE.md/COMMANDS.md | — |
| 2 | create-test-plan | NOT_STARTED | — | — | — | — |
| 3 | analyze-requirements | COMPLETED | 2026-07-29 | v1.0 | 5 file `02_analyze-requirements/v1.0/` + MASTER-MEMORY.md | **Update 9 (2026-07-29, chuẩn bị merge phần "chi tiết + flow" từ QC khác trong team — bổ sung Trang chủ + Bảng tin):** Team 2 người: người còn lại (anhdc4) tự phân tích + generate TC trên project riêng (`02_analyze-requirements CA`, dựa trên demo HTML) — quyết định lấy project GiangDC2 làm base, gộp thành 1 project tổng. Trước khi generate-tc cho các màn còn thiếu (Chi tiết tin/Theo dõi đơn/Huỷ đơn/Tặng quà — vốn đã có sẵn scenario ASN/DLV/GIFT/CNL, chỉ chưa generate TC), rà lại 2 màn dùng chung Trang chủ + Bảng tin: `DOC-v1.0-02 §2/§3.1/§3.3` (đã đăng ký sẵn từ INIT) mô tả đủ Header/Banner/Card/bottom-nav (Trang chủ) và Danh sách card (Bảng tin) nhưng CHƯA từng capture thành Block Definition/scenario riêng. Bổ sung +2 REQ (`REQ-ORD-015`, `REQ-ASN-010`), +2 scenario (`SC-ORD-033`, `SC-ASN-014`), +2 Block Definition mới. TC của CA chỉ dùng làm checklist field tham khảo — Source Quote/Location trích trực tiếp từ DOC-v1.0-02, không trích từ TC CA (Project_rule.md §10.1). Nhân tiện fix stale: dòng "Tổng SC" ở §1 Version Registry (87, dừng từ Update 8) chưa từng sync theo các Update 9-13 sau đó trong `MEMORY.md` v1.0 (đã lên 90 trước đợt này) — nay đồng bộ lại đúng **92** (=90 + 2 scenario mới). **Chưa cross-check ảnh Figma (DOC-v1.0-04)/app STG cho 2 block mới** — cần vibe-test trước khi coi Expected Result final. Đồng bộ `MEMORY.md` (§0 changelog #14, §3, §4, §4.1), `test_scenario_map.md` (Block Definitions + Scenarios table + Source Detail), `requirement_traceability.md` (§1, §2 Coverage Summary). **Update 8 (2026-07-28, QA xác nhận trực tiếp qua ảnh Figma DOC-v1.0-04 + UI thật):** (1) Block "2 lựa chọn" màn "Đăng tin mới" (`test_scenario_map.md`) thiếu 3 field so với UI thật — bổ sung subtitle "Bạn muốn làm gì?", banner cam kết "App không thu phí, không chat, không thanh toán. Sau khi ghép, SĐT hai bên sẽ được lộ để liên hệ ngoài app.", và mô tả verbatim card "Tôi nhận giao hàng" (xác nhận qua ảnh Figma hash `f821ba3087b8cc6e8065fbde6e327274d34482b2`) — block này nay có đủ 5 field thay vì 2. (2) QA GiangDC2 xác nhận trực tiếp 2 clarification trước đó ở dạng đề xuất/suy luận nay đúng là hành vi/text THẬT trên UI app STG: **C-ORD-06** (empty state "Hiện tại chưa có dữ liệu") và **C-NTF-03** (đánh dấu đã đọc tap-1-item/mark-all + scroll infinite-load) — cả 2 chuyển từ Open sang **Resolved**. Đồng bộ `MEMORY.md` (§0 changelog #9, §4 Scenario Index, §6 Clarifications), `test_scenario_map.md` (Block Definitions + toàn bộ Source Quote/Analyst Note liên quan), `requirement_traceability.md` (REQ-GIFT-001, REQ-NTF-001). Không phát sinh scenario mới — thuần xác nhận/nâng status + bổ sung field bị sót ở tầng phân tích, chưa đụng tới TC Excel (xem `generate-tc` §8 để cập nhật TC tương ứng). **Update 7 (2026-07-28, BRD v3.2 mới — `00_input/v1.0/27072026/FoxEco BRD v3.2.md`):** Diff dòng-đối-dòng xác nhận v3.2 CHỈ thêm 1 section mới `§D8 Validate & Giá trị mặc định (Form Rules)` so với v3.1 (toàn bộ §A1-A10/D1-D7 giữ nguyên; `FoxEco Design v3.2.html` cũng không đổi nội dung, chỉ khác UUID bundler nội bộ — đã xác nhận bằng byte-diff). D8 resolve dứt điểm phần "TBD" còn lại của `C-ORD-01` (maxlength cụ thể: Ghi chú 300 ký tự, Địa chỉ 200 ký tự, Tên người nhận 2-60 ký tự, khung giờ tối thiểu 30 phút) + hé lộ cơ chế categorical của field "Giá trị hàng" (chọn "Cao" → cảnh báo trách nhiệm tự thoả thuận — KHÁC với ngưỡng số tiền BR-ORD-03 vẫn Deferred ở `C-ORD-02`, đã ghi rõ note phân biệt 2 cơ chế). +2 REQ (`REQ-ORD-012`, `REQ-ORD-013`), +4 scenario ORD (`SC-ORD-027..030`), +1 scenario CNL (`SC-CNL-005`, lý do huỷ min 5 ký tự — VAL-04), 1 scenario MODIFIED (`SC-ORD-013`, un-deferred). Cũng phát hiện: dòng "Chờ BA bổ sung" về tham số khớp tuyến (bán kính/độ lệch khung giờ/chu kỳ quét) cuối §D7 đã bị XOÁ khỏi v3.2 nhưng KHÔNG kèm câu trả lời — `C-NTF-02` giữ nguyên Partially Resolved (không tự nâng status). Đồng bộ đủ 5 file version (`MEMORY.md`, `requirement_traceability.md`, `test_scenario_map.md`, `test_data_catalog.md`, `risk_assessment.md`) + `MASTER-MEMORY.md` (§1/§3, nhân tiện fix luôn §3 bị stale từ đợt health-check trước — chưa từng sync sau UPDATE 6) + root `CLAUDE.md`. Tổng: **82→87 scenario** (P1:20·P2:39·P3:28 — nhân tiện fix ORD module row P1 4→5, vốn lệch từ trước), **41→43 REQ** (nhân tiện fix dòng "Tổng" ở `MEMORY.md §3` vốn ghi 40 dù per-module cộng ra 41 từ trước, không liên quan BRD v3.2). **Lịch sử trước đó — UPDATE #1 (2026-07-24):** bổ sung DOC-v1.0-04 (Fox Eco Doc, 82 ảnh Figma). Kết quả: resolve C-DLV-01 (Receiver-only) + C-DLV-03 (modal đơn giản); Partially Resolved C-USR-01 (có tier text, không điểm số) + C-GIFT-01 (rating tồn tại ở notification, chưa rõ màn thao tác); +1 clarification mới C-ORD-05 (2 biến thể "Đăng tin thành công" có/không "Mã tin"); cập nhật verbatim text/button chính xác cao cho NTF/USR/GIFT/DLV/CNL/ORD screens. Không phát sinh scenario mới. UPDATE #2 (cùng ngày): +1 clarification mới C-USR-03 (Resolved) — QA xác nhận app STG thật KHÔNG có chức năng cập nhật hồ sơ cá nhân (view-only), sửa lại scope REQ-USR-002/SC-USR-002. **UPDATE #3 (2026-07-27):** BA/PO trả lời batch 16 clarification qua chat trực tiếp. Resolved — Deferred (out of scope v1.0): C-ORD-02 (ngưỡng giá trị hàng, gỡ BLOCKER cứng), C-USR-01 (tier/hạng), C-USR-02 (cấu hình kênh liên hệ), C-GIFT-01 (rating sao), C-TS-01 (Admin Portal), C-CNL-01 (Báo sự cố). Resolved — rule mới áp dụng ngay: C-ORD-01 (validate bắt buộc + maxlength TBD), C-ORD-03 (hạn tin = giá trị user chọn), C-ORD-04 (không chặn Thuốc/Y tế), C-ASN-01 (SĐT lộ sau ghép), C-ASN-02 (cấm tự nhận), C-DLV-03 (modal đơn giản chính thức). Partially Resolved: C-NTF-02 (khớp tuyến = địa chỉ + khung giờ). Còn Open thực sự: C-ORD-05 (mã tin), C-NTF-01 (bổ sung bảng unified 3 nguồn, chờ BA chọn), C-DLV-02 (default vị trí GPS). Đồng bộ 4 file (`MEMORY.md`, `requirement_traceability.md`, `risk_assessment.md`, `test_scenario_map.md`) + root `CLAUDE.md`. **UPDATE #4 (2026-07-27, rescan UI riêng màn Thông báo):** rà lại `00_input/v1.0/27072026/` (BRD v3.2, Design v3.2, 1 ảnh chụp app STG) theo yêu cầu user — không có bằng chứng UI mới cho màn Thông báo. Theo yêu cầu user (empty state "tương tự chức năng khác đang có"), +1 scenario mới `SC-NTF-007` (empty state danh sách thông báo), mở rộng phạm vi `C-ORD-06` sang `REQ-NTF-001`. Đồng bộ `MEMORY.md` (§0 changelog, §3 Module Summary, §4 Scenario Index, §4.1 REQ-NTF-001 Analyst Note, §6 Clarifications), `test_scenario_map.md` (block "Empty state" + SC-NTF-007 + Source Detail), `requirement_traceability.md` (REQ-NTF-001 row + §2 Coverage Summary). `risk_assessment.md` không đổi (không tạo RISK row riêng, theo đúng tiền lệ C-ORD-06 ở ORD/GIFT). **Update 5 (2026-07-27, gap phát hiện khi review TC-NTF):** +2 scenario `SC-NTF-008` (đánh dấu đã đọc), `SC-NTF-009` (scroll load thêm dữ liệu), +1 clarification `C-NTF-03` (Open). **Update 6 (2026-07-27, fix finding health-check — data repair):** `SC-ORD-015..026` (12 scenario, màn Hoạt động — đã có sẵn trong `test_scenario_map.md`/`requirement_traceability.md` từ phiên generate-tc trước) bị thiếu khỏi `MEMORY.md` §4 Scenario Index — đã bổ sung đủ 12 row. Recompute §3 Tổng row + §1 Version Registry + `requirement_traceability.md` §2 Coverage Summary + `CLAUDE.md`: **65→82 scenario** (P1:19·P2:37·P3:26). Không tạo scenario mới — thuần sync dữ liệu đã tồn tại. |
| 4 | generate-tc | COMPLETED | 2026-07-29 | v1.0 (4 screen: Hoạt động, Cá nhân, Thông báo, Đăng tin) | `fragments/TC-HOATDONG-v1.0.xlsx` (20 TC, comprehensive + Coverage Matrix), `fragments/TC-CANHAN-v1.0.xlsx` (15 TC, comprehensive + Coverage Matrix), `fragments/TC-NTF-v1.0.xlsx` (27 TC, comprehensive mode + Coverage Matrix), `fragments/TC-DANGTIN-v1.0.xlsx` (109 TC, comprehensive mode + Coverage Matrix) | **Update 10 (2026-07-29, PRUNE/DEDUPE — chi tiết xem `03_test-cases/v1.0/CHANGELOG.md`):** user yêu cầu bỏ khối TC trùng lặp "Trang chủ, Hoạt động — Tin tự động Hết hạn" (`TC_04.110-113`, 4 TC cuối sheet Đăng tin) — nội dung trùng hoàn toàn với TC đã có ở `fragments/TC-HOATDONG-v1.0.xlsx` (rows 12-19, 26-28). Xoá 6 row (2 group/sub-block header + 4 TC) khỏi sheet `Test Cases` (fragment) và `Test Case 4` (3 file TC-MASTER), cập nhật `Coverage Matrix - Đăng tin`/`Coverage Matrix` (SC-ORD-003 Total 5→4, SC-ORD-005 Total 3→0 kèm note dedupe). Re-sync alias byte-identical + LibreOffice headless recalc verify: Dashboard Đăng tin 113→**109 TC**, Tổng TC-MASTER 175→**171 TC**, RTM REQ-ORD-004 vẫn 2 TC (qua Hoạt động, không orphan). Không đổi `test_scenario_map.md`/`requirement_traceability.md` (SC-ORD-003/005 vẫn hợp lệ, chỉ dedupe TC). §8 generate-tc vẫn **COMPLETED**. Lịch sử trước — **Update 9 (2026-07-29, chi tiết xem `03_test-cases/v1.0/CHANGELOG.md` + `MEMORY.md §9`):** (1) Đăng tin +13 TC mới chèn đúng vị trí Block Người gửi/Người nhận trong Wizard B2/B3 (Tên = read-only, SĐT/Địa chỉ = editable+required+validate, nguồn `REQ-ORD-014`/`SC-ORD-031`/`SC-ORD-032`, autocomplete văn phòng theo text nhập) — 100→113 TC. (2) Thông báo: viết lại `TC_03.1-3` từ hướng "trần thông báo/ngày mock N=3" sang đúng cơ chế thật "trần tối đa 5 thông báo/1 tin OFFER, không cộng dồn theo ngày" (BA xác nhận rõ qua chat) — gán lại nguồn từ REQ-NTF-002 sang REQ-ASN-005/`SC-ASN-013`, `SC-NTF-006` (trần/ngày) đánh dấu DEPRECATED/superseded — 28→27 TC. (3) **SYNC** (`/generate-tc` — user yêu cầu "cập nhật testcase master"): phát hiện file chính `ISC_FoxEco_v1.0_TC_v1_R1.xlsx` (theo naming convention QA) bị lệch so với alias `TC-MASTER-v1.0.xlsx`/`TC-MASTER-LATEST.xlsx` — thiếu đúng 13 TC Đăng tin mới nhất ở (1) vì 2 update trên chỉ ghi vào alias, bỏ sót file chính; đã rebuild lại file chính từ nội dung alias mới nhất, re-copy cả 2 alias — cả 3 file giờ byte-identical (md5 khớp). Nhân tiện patch nốt 3 ô Expected Result còn sót hedge text cũ "⚠ QA đề xuất, chưa BA confirm chính thức" (Hoạt động dòng 27-28 sheet `Test Cases`, Thông báo dòng 29 sheet `Test Case 3`) sang text đã Resolved khớp fragment (C-ORD-06) — sót lại từ đợt update Remark 2026-07-28. Hoàn thiện Revision History: điền nốt Change Description còn trống ở row Version 2, thêm row Version 3 cho sync này. Tổng TC-MASTER: 162→**175 TC** (20+15+27+113). §8 generate-tc = **COMPLETED**. Lịch sử trước — **Update 8 (2026-07-28, CONSOLIDATE — thêm module Đăng tin vào TC-MASTER):** gộp fragment TC-DANGTIN (100 TC) vào `ISC_FoxEco_v1.0_TC_v1_R1.xlsx` hiện có (3 sheet cũ) → sheet mới `Test Case 4` (Mã CN TC_04) + `Coverage Matrix - Đăng tin`. Dashboard +1 row (TC_04/100 TC). RTM: nối thêm term `Test Case 4` vào 4 công thức của `REQ-ORD-004` (đã có row từ trước) + thêm **11 row Req ID mới** (REQ-ORD-001/002/003/005/006/007/008/009/010/012/013) — RTM từ 10→21 Req ID, unmerge+insert_rows+re-merge đúng quy trình (note row B18:J18→B29:J29). Revision History +1 row (version 2). **Bug phát hiện + fix ngay lúc consolidate (mới, khác 2 bug đã biết từ lần consolidate trước):** Dashboard cột F "Tổng TC" dùng `COUNTIF(...$A$8:$A$500,"?*")` — công thức cũ chỉ trừ đúng 1 phantom row (Screen-label đầu tiên tại row 7, do range bắt đầu từ $A$8), nhưng **Đăng tin có 9 Screen riêng biệt** (không phải 1 Screen/nhiều Block như Hoạt động/Cá nhân/Thông báo) → 8 Screen-label còn lại (rows 12/31/71/88/94/99/111/121) vẫn có text ở cột A, bị đếm nhầm thành "TC ma" → Dashboard báo sai **108 thay vì 100 TC thật**. Fix: đổi toàn bộ 4 row Dashboard (không chỉ riêng TC_04) sang đếm cột C (DOC Source) thay vì cột A — cột C CHẮC CHẮN rỗng ở MỌI row label (Screen lẫn Block) bất kể vị trí, nên robust với sheet có bất kỳ số lượng Screen nào, không chỉ đúng cho trường hợp 1-phantom-row như fix cũ. Verify bằng LibreOffice headless recalc 2 lần (trước/sau fix): Dashboard 20/15/27/100 (đúng cả 4), RTM Tổng E27=188 (tổng dồn theo Req ID, đúng vì 1 TC nhiều Req vẫn đếm nhiều lần theo thiết kế sheet), không orphan Req ID. **Lưu ý cho lần consolidate sau:** nếu module mới có ĐA Screen trong 1 sheet, PHẢI dùng công thức Dashboard dựa cột C (đã fix), không dùng lại công thức cột A kiểu cũ dù đã "trừ 1 row" — nên đưa fix này vào `consolidate.md` chính thức (chưa sửa file skill lần này, ngoài phạm vi yêu cầu user). Tổng TC-MASTER: 63→**162 TC** (20+15+27+100). §8 generate-tc = **COMPLETED**. Lịch sử trước — **Update 7 (2026-07-28, GENERATE comprehensive — Đăng tin, Mã CN TC_04):** user yêu cầu tổng hợp rồi generate TC cho toàn bộ luồng "Đăng tin" (2 nhóm: Wizard NEED "Tôi cần gửi hàng" B1-B2-B3 + Form OFFER "Tôi nhận giao hàng" 1 bước) theo `--mode comprehensive`. Scope 17 scenario nguồn: SC-ORD-001/002/003/005/006/007/008/009/010/011/012/013/014/027/028/029/030 (SC-ORD-004 "Theo dõi đơn timeline" và SC-ORD-015..026 "màn Hoạt động" NGOÀI scope — đã có fragment riêng TC-HOATDONG). Rubric per scenario: B1 EP (chip Loại hàng/Giá trị hàng 3 partition, EP field bắt buộc rỗng, 2 nút popup kết quả, EP so-sánh-field địa chỉ trùng); B2 BVA (Ghi chú 300, Tên người nhận 2-60, Địa chỉ 200, Điểm xuất phát OFFER 200, ảnh 5MB, khung giờ tối thiểu 30 phút — cả NEED lẫn OFFER); B3 DT (nút submit disabled: 3 rule sau collapse — VAL-01); B4 ST (điều hướng wizard B1↔B2↔B3, khoá nút Chỉnh sửa từ MATCHED/IN_TRANSIT/COMPLETED/EXPIRED, vòng đời POSTED→EXPIRED + 2 guard negative); B6 EG (10-pattern email người nhận ở SC-012, VAL-03 trim/chuẩn hoá SĐT). B5 PW/B7 CRUD/B8 CEG: N/A toàn module (lý do log riêng trong Coverage Matrix footer — không đủ 3 tham số nhân; CRUD tách theo module khác Delete→CNL/List→TC-HOATDONG; DT chỉ 3 rule không vượt ngưỡng escalate CEG). +10 TC field/column completeness (Step 3b) cho 6 block form (B1 thông tin hàng, B1 8-chip Loại hàng, B2 Người gửi, B2 6-preset văn phòng, B2 Người nhận, B2 Thời gian, B3 4-thành-phần, popup Đăng tin thành công, Form OFFER 7-field, form Chỉnh sửa). Kết quả: 17 scenario → **94 TC** (High:4, Medium:58, Low:32), + sheet `Coverage Matrix` (17 hàng scenario × 8 cột technique + Total/Coverage% — 4 SC log 0-dim có lý do: SC-002/006/010/011). Compliance gate: reconcile tay xác nhận tổng TC per-scenario trong Coverage Matrix = đúng TC thật trong sheet (assert tự động trong script generate, không lệch dòng nào). **Update 7.1 (2026-07-28, fix findings từ `/review-tc --module ORD`):** module review (direct mode, score 85/100 CONDITIONAL capped) phát hiện 2 Major + 1 Info, user yêu cầu fix cả 3 → REGENERATE: (1) tách toàn bộ Steps/Expected về đúng 1:1 theo `generate.md` (trước đó 66/94 TC dùng dòng Expected gộp range kiểu "1-2. ..."); (2) bổ sung TC completeness "Check đủ 2 lựa chọn hiển thị đúng nhãn" cho Block "2 lựa chọn" màn Đăng tin mới (Step 3b, trước đó thiếu); (3) bổ sung 3 TC mới cho `SC-ORD-004` (P1, "Timeline tin ghi nhận đầy đủ mốc thời gian" — trước đó 0 TC ở BẤT KỲ fragment nào trong toàn project) qua Screen mới "Theo dõi đơn (Sender) — Timeline trạng thái". Tổng 94→98 TC (High:5, Medium:61, Low:32), Coverage Matrix 17→18 scenario row. Recheck: 0 finding còn lại (raw 100/100), nhưng **score vẫn cap 85/100 CONDITIONAL** theo đúng rule fallback trực tiếp (không có `review-agent/AGENT.md`) — cap áp dụng bất kể kết quả, không phải do còn lỗi. **Update 7.2 (2026-07-28, user tự rà lại completeness per-màn-hình, phát hiện thêm 2 gap):** Block "Đơn của tôi" (Trang chủ, 6 field — SC-ORD-003/004 tham chiếu) và Block "Form điền sẵn" (Chỉnh sửa tin — thiếu "đủ 2 nút Cập nhật/Huỷ chỉnh sửa") chưa có TC completeness → bổ sung 2 TC. **Bỏ qua màn "Chi tiết tin" theo yêu cầu user (Block "Thông tin hàng" 3 field + "Lộ trình & liên hệ" 4 field, tham chiếu bởi SC-ORD-001/SC-ASN-001/003) — CHƯA có TC nào (kể cả completeness) ở bất kỳ đâu trong project; user xác nhận sẽ có bộ TC riêng cho màn này sau, KHÔNG thuộc phạm vi TC-DANGTIN.** Tổng 98→**100 TC** (High:5, Medium:63, Low:32). Chưa consolidate vào TC-MASTER — §8 generate-tc chuyển về **PARTIAL** cho tới khi chạy `/generate-tc --sync` hoặc `--consolidate`. Lịch sử trước đó — **Update 5 (2026-07-28, REGENERATE comprehensive — Hoạt động + Cá nhân):** user yêu cầu viết lại 2 fragment còn lại (đang ở standard mode) sang `--mode comprehensive`. Rubric per scenario (10 SC màn Cá nhân, 13 SC màn Hoạt động — bao gồm SC-ORD-004/005 dùng chung): phần lớn scenario 0-dim (view-only/navigation, không input field/state lifecycle/multi-condition) → N/A log rõ theo từng cột B1-B8; áp dụng được B1 EP (SC-USR-004 N/A do không phải enum; SC-GIFT-003 phân vùng loại quà đã/chưa nhận; SC-ORD-017/021 phân vùng theo trạng thái đơn Đã ghép/Đang giao/Hoàn thành; SC-ORD-023 tách empty-state theo tab kiểu Project_rule §10.2) + B2 BVA (SC-USR-004 chỉ số 0/1/large; SC-GIFT-003 số lượng quà lớn; SC-ORD-004/005 boundary trước hạn tin). B3/B4/B5/B6/B7/B8 N/A toàn bộ (không có input field tự do, không multi-condition, <3 param, không CRUD trực tiếp trên 2 màn này; B4 ST chỉ tham chiếu cross-scope sang ASN/Bảng tin/CNL, không lặp TC). Kết quả: Cá nhân 10→**15 TC** (Medium:5, Low:10), Hoạt động 13→**20 TC** (High:1, Medium:12, Low:7), mỗi fragment +1 sheet `Coverage Matrix`. Tiện thể phát hiện + sửa data-repair: SC-USR-002..007 và SC-GIFT-003/005/006/007 từng có TC (standard mode, 2026-07-27) nhưng bị thiếu cờ "✅ Đã tạo TC" ở `MEMORY.md §4` — đã sync lại. Compliance gate: mọi technique applicable đều sinh TC hoặc log N/A/dedupe rõ lý do, không TC nào bị bỏ sót âm thầm. Chưa consolidate vào TC-MASTER. Lịch sử trước đó: Pilot **screen-based sheet** (1 sheet/màn hình thay vì 1 sheet/module) theo đề xuất user — Mã chức năng TC_01/TC_02. Trước khi generate đã bổ sung REQ-ORD-011 + SC-ORD-015..026 (màn Hoạt động) và SC-USR-006/007 + SC-GIFT-005/006/007 (Cá nhân ↔ Hoạt động/Quà đã nhận, nguồn quan sát thực tế app STG) vào `test_scenario_map.md`/`MEMORY.md`/`requirement_traceability.md`. **Update 1:** tách SC-ORD-017 ("chuyển tab" gộp) thành 2 TC riêng theo Project_rule.md §10.2 mới (mỗi tab data 1 TC). **Update 2:** viết lại SC-GIFT-003 — card "Quà đã nhận" hiển thị CÓ ĐIỀU KIỆN theo data (chỉ loại đã nhận), không phải completeness cứng đủ 4 loại như hiểu ban đầu; xác nhận "Quà đã nhận" là màn riêng (không phải section trong Cá nhân), thêm điều hướng menu + icon quay lại + empty state (C-ORD-06, mở rộng phạm vi). **Update 3 (2026-07-27, màn Thông báo, Mã CN TC_03):** generate 12 TC cho SC-NTF-001..007 (standard mode) — tách theo actor/event (Sender/Receiver/Carrier riêng cho từng NTF-01..09), +1 TC field/column completeness (card structure: icon/title/nội dung/thời gian + header/nhóm thời gian/nút Đánh dấu đã đọc — Step 3b) và +1 TC empty state (SC-NTF-007). Expected Result ưu tiên verbatim DOC-v1.0-04 (Figma) khi có bằng chứng ảnh xác nhận khớp, fallback DOC-v1.0-01 (baseline BA D6) cho các case chưa có ảnh (NTF-01/04/05/06/09); TC-06 (hoàn tất) không assert phần rating (C-GIFT-01, out of scope). **Update 4 (2026-07-27, REGENERATE comprehensive mode):** user phát hiện gap khi review (thiếu TC đánh dấu đã đọc + scroll load thêm) → bổ sung SC-NTF-008/009 + clarification C-NTF-03 vào analyze-requirements trước, sau đó regenerate toàn bộ TC-NTF-v1.0.xlsx với `--mode comprehensive`: rubric per 9 scenario (SC-NTF-001..009) → áp dụng B1 EP (actor/huỷ-role partitions), B2 BVA (trần thông báo/ngày: min-1/at-cap/max+1; scroll pagination: page1/page2/last-page/over-scroll), B4 ST (milestone lifecycle MATCHED→IN_TRANSIT→DELIVERED→COMPLETED: 3 valid + 3 invalid; đánh dấu đã đọc unread↔read: 2 valid + 2 idempotent); B3/B5/B6/B7/B8 N/A toàn bộ 9 scenario (lý do log rõ per-cột, không silent-skip). Kết quả: 12 → **28 TC** (Medium:14, Low:14) + sheet phụ `Coverage Matrix` (9 hàng scenario × 8 cột technique + Total/Coverage%). Compliance gate: mọi technique applicable đều sinh TC hoặc log N/A, không TC nào bị bỏ sót âm thầm. **Update 6 (2026-07-28, CONSOLIDATE):** gộp 3 fragment → `ISC_FoxEco_v1.0_TC_v1_R1.xlsx` (+ alias TC-MASTER-v1.0.xlsx/TC-MASTER-LATEST.xlsx) — Dashboard (3 row TC_01/02/03), RTM (10 Req ID, xem §6 MASTER-MEMORY), Coverage Matrix ×3 (đổi tên `Coverage Matrix - Cá nhân`/`- Thông báo` để tránh trùng). Verify bằng LibreOffice headless recalc (TC ID, Dashboard Tổng TC, RTM đều đúng), không orphan Req ID. **2 bug phát hiện + fix ngay lúc consolidate (chỉ lộ ra khi có Dashboard/RTM thật, fragment đơn lẻ không thấy):** (1) Dashboard cột F "Tổng TC" mặc định đếm cột A từ `$A$7` — nhưng convention Screen-label-row (generate.md Step 6.4, cột A chứa text tên màn ở row 7) khiến công thức đếm dư 1 "TC ma" mỗi sheet; đã sửa range → `$A$8:$A$500` cho cả 3 row Dashboard (ngoại lệ hẹp, có log lý do). (2) RTM có 2 merge cell mồ côi từ template gốc (`C11:D11` của row Tổng mẫu, `B13:J13` của dòng ghi chú) neo cứng ở row cũ — `ws.insert_rows()` của openpyxl KHÔNG tự dịch merge theo, nên khi chèn thêm row cho đủ 10 Req ID thật, 2 merge này đè lên đúng row REQ-ORD-011/REQ-CNL-001 mới, làm rỗng âm thầm 1 số cột (D11, C13:J13) dù ghi giá trị không lỗi — phải unmerge trước khi insert_rows rồi dựng lại dòng ghi chú ở vị trí mới sau khi ghi xong. **Lưu ý cho lần consolidate sau:** nếu thêm module mới cũng dùng Screen-label-row convention, nhớ áp lại fix (1); nếu RTM cần insert thêm row nữa, kiểm tra lại merged_cells trước khi insert_rows (fix 2) — 2 case này nên đưa vào `consolidate.md` Step 3/Step 4 checklist chính thức trong lần cập nhật skill sau (chưa sửa file skill lần này, ngoài phạm vi yêu cầu user). §8 generate-tc = COMPLETED tại thời điểm đó (3 fragment), tạm chuyển PARTIAL sau Update 7 (fragment Đăng tin thứ 4 chưa consolidate), rồi quay lại **COMPLETED** sau Update 8 (đã consolidate đủ 4 module — xem đầu dòng này). |
| 5 | review-tc | COMPLETED | 2026-07-29 | v1.0 (175 TC, 4 sheet) | `11_tc-review/review-report-v1.0.md` — Score gốc 70/100, sau fix ~91/100 (2 finding còn treo có lý do rõ ràng, không phải sót) | **Update (2026-07-29, re-review + FIX ngay theo yêu cầu user, trừ scope Huỷ đơn/Chi tiết — QC khác phụ trách):** scope 175 TC/4 sheet. Đã **fix trực tiếp** 3/4 Critical + 2/3 Major + 1/1 Minor (xem `CHANGELOG.md` dòng cuối + report để có tag FIXED/SKIPPED/NOT FIXED per finding): (1) [FIXED] xoá round data mẫu 17 dòng nhiễm (14 TC + 3 row Block-label) ở `Test Case 3`; (2) [FIXED] sửa formula RTM cột "Đã chạy" (bug `COUNTIFS(...,"<>")` đếm nhầm ô công thức-rỗng — đổi sang SUMPRODUCT `AO<>""`), verify recalc: Tổng Đã chạy nay đúng 0/201 (trước sai 192/192=100%); (3) [FIXED] thêm 2 row RTM thiếu (`REQ-ASN-005` Số TC=3, `REQ-ORD-014` Số TC=6) — Tổng Số TC 192→**201**, RTM nay 23 Req ID; (4) [FIXED] Group cột D "Business Rule"→"Functional" (5 TC); (5) [FIXED] Tap→Bấm (7 TC). **[SKIPPED theo yêu cầu user]** `REQ-CNL-001`/3 scenario Huỷ đơn (`SC-CNL-001/003/005`) — QC khác đang viết TC riêng cho màn Chi tiết tin + Huỷ đơn, ngoài phạm vi. **[NO_CHANGE — chấp nhận convention]** R1-04 Steps/Expected lệch 1:1 (57 TC) — không rewrite, chấp nhận pattern "step setup không cần expected riêng" làm chuẩn chính thức. **[NOT FIXED — cần generate-tc riêng]** `REQ-NTF-002` phần OPR-07 (lộ liên hệ có kiểm soát) — 0 TC, cần derive scenario mới trước, không phải fix cơ học. Đã áp dụng đồng bộ trên 3 file chính (`TC-MASTER-v1.0.xlsx`/`TC-MASTER-LATEST.xlsx`/`ISC_FoxEco_v1.0_TC_v1_R1.xlsx`) + 3 fragment liên quan (`TC-NTF`, `TC-DANGTIN`, `TC-HOATDONG`). Khuyến nghị `/review-tc --recheck` sau khi module Huỷ đơn/Chi tiết của QC kia hoàn tất, để có score chính thức. |
| 6 | scan-source-code | NOT_STARTED | — | — | — | N/A (no automation) |
| 7 | implement-automation | NOT_STARTED | — | — | — | N/A (no automation) |
| 8 | review-src-tc | NOT_STARTED | — | — | — | N/A (no automation) |
| 9 | vibe-test | NOT_STARTED | — | — | — | — |
| 10 | execute-maintain | NOT_STARTED | — | — | — | N/A (no automation) |
| 11 | log-bug | NOT_STARTED | — | — | — | — |
| 12 | test-report | NOT_STARTED | — | — | — | — |
| 13 | health-check | COMPLETED | 2026-07-27 | Quick | 0 CRITICAL, 3 WARNING, 1 INFO | **Finding chính (chưa fix):** SC-ORD-015..026 (12 scenario, màn Hoạt động) tồn tại trong `test_scenario_map.md` + được `requirement_traceability.md` REQ-ORD-011 reference đúng, nhưng **THIẾU hoàn toàn khỏi `MEMORY.md` §4 Scenario Index** (chỉ có 70/82 SC thật). Root cause: session generate-tc thêm scenario nhưng quên sync §4. Cascade: §3 Tổng row (65, đúng ra 82), MASTER-MEMORY §1 (65, đúng ra 82), requirement_traceability §2 Coverage Summary (65/65, đúng ra 82/82), CLAUDE.md dòng tổng scenario (63, đúng ra 82) — TC-HOATDONG-v1.0.xlsx (13 TC) vẫn tồn tại và map đúng REQ-ORD-011 nên KHÔNG mất TC, chỉ mất traceability ở MEMORY §4. Suggest: `/analyze-requirements --update "sync SC-ORD-015..026 vào MEMORY.md §4 + recompute Tổng/priority"`. Priority breakdown cũng lệch theo (true P1:19·P2:37·P3:26=82, MEMORY §3 ghi 19/29/17=65). |

## 9. Notes
- 2026-07-24: INIT analyze v1.0 hoàn tất từ 3 tài liệu (BRD v3.1 + PRD tái dựng từ demo + prototype tương tác). Phát hiện ban đầu 4 clarification cần BA/PO xác nhận: (1) C-USR-01 tier/điểm ECO/CO2, (2) C-ORD-02 ngưỡng giá trị hàng, (3) C-DLV-01 ai được xác nhận "Đã nhận", (4) C-GIFT-01 tính năng đánh giá sao (RAT-01/02).
- **Cập nhật 2026-07-24 (sau đợt bổ sung DOC-v1.0-04 + health-check):** chỉ còn **1 BLOCKER cứng — `C-ORD-02`** (Open, chưa có con số ngưỡng giá trị hàng). 3 điểm còn lại đã hạ mức: `C-DLV-01` = **Resolved** (Receiver-only, xác nhận qua ảnh Figma), `C-USR-01` = **Partially Resolved** (có tier text, không điểm số — không còn BLOCKER cứng cho SC-USR-004), `C-GIFT-01` = **Partially Resolved** (rating tồn tại ở notification, chưa rõ màn thao tác — vẫn Blocked riêng cho REQ-GIFT-002 nhưng không chặn toàn bộ module GIFT). Chi tiết đầy đủ + status mới nhất từng clarification: `02_analyze-requirements/v1.0/MEMORY.md §6`.
- **Cập nhật 2026-07-27 (BA/PO trả lời batch 16 clarification qua chat trực tiếp — QA GiangDC2 tổng hợp):** **BLOCKER cứng `C-ORD-02` đã gỡ** — BA/PO xác nhận ngưỡng giá trị hàng là phase sau, out of scope v1.0 (Resolved — Deferred). Cùng đợt, resolve/deferred thêm 12 clarification: C-USR-01 (tier — deferred), C-USR-02 (kênh liên hệ — deferred), C-GIFT-01 (rating sao — deferred), C-TS-01 (Admin Portal — deferred), C-CNL-01 (Báo sự cố — deferred), C-ORD-01 (validate bắt buộc — có rule, maxlength TBD), C-ORD-03 (hạn tin = giá trị user chọn), C-ORD-04 (không chặn Thuốc/Y tế ở v1.0), C-ASN-01 (SĐT lộ sau ghép — chốt rule, app hiện tại có thể cần fix), C-ASN-02 (cấm tự nhận — chốt rule, app hiện tại có thể cần fix), C-DLV-03 (modal đơn giản chính thức, form đầy đủ out of scope), C-NTF-02 (Partially Resolved — khớp tuyến = địa chỉ + khung giờ, còn thiếu độ lệch khung giờ/chu kỳ quét).
  - **Còn Open thực sự (chưa có câu trả lời), cần theo dõi tiếp:** `C-ORD-05` (biến thể "Mã tin" ở màn Đăng tin thành công), `C-NTF-01` (danh sách thông báo chính thức — đã bổ sung bảng unified 3 nguồn trong `MEMORY.md §6.1`, chờ BA chọn), `C-DLV-02` (default bật/tắt chia sẻ vị trí — phase sau nhưng chưa có giá trị), phần maxlength cụ thể của `C-ORD-01` (follow-up).
  - Đã đồng bộ 4 file: `MEMORY.md` (§6 + §6.1 + bảng unified NTF), `requirement_traceability.md` (§2 Coverage Summary + §3 status headers), `risk_assessment.md` (hạ mức risk USR/ORD/GIFT/CNL/TS, giữ nguyên High cho ASN regression), `test_scenario_map.md` (Given/When/Then + Analyst Note cho các SC bị ảnh hưởng, SC-ORD-013 đánh dấu Deferred — không derive TC). Không tạo scenario mới, không xoá scenario nào.
- Chưa có `create-test-plan` (Test Plan §01) — generate-tc/review-tc vẫn chạy được độc lập vì không phải hard prerequisite, nhưng khuyến nghị tạo Test Plan trước để xác định rõ scope/exit criteria cho FoxEco v1.0.
- **Cập nhật 2026-07-28 (BA cập nhật BRD lên v3.2):** so với v3.1, v3.2 CHỈ thêm 1 section mới `§D8 Validate & Giá trị mặc định (Form Rules)` — resolve dứt điểm phần maxlength TBD của `C-ORD-01` (nay Resolved đầy đủ) + hé lộ cơ chế categorical "Giá trị hàng = Cao → cảnh báo" (khác với ngưỡng số tiền BR-ORD-03 vẫn Deferred ở `C-ORD-02`). +2 REQ, +5 scenario (4 ORD + 1 CNL), 1 scenario modified (SC-ORD-013 un-deferred). `C-NTF-02` vẫn Partially Resolved — dòng "Chờ BA bổ sung" tham số khớp tuyến ở §D7 bị xoá khỏi v3.2 nhưng không có câu trả lời thay thế. Chi tiết đầy đủ: `02_analyze-requirements/v1.0/MEMORY.md` header + §6.1 (C-ORD-01, C-ORD-02, C-NTF-02).
- **Cập nhật 2026-07-29 (rà soát DOC Source "Quan sát thực tế app" trong TC-MASTER):** user nghi ngờ 42 dòng TC (4 sheet) gắn nhãn "Quan sát thực tế app" thực ra là câu trả lời BA bị bỏ sót khi quét tài liệu. Xác minh: ảnh cũ trích cho REQ-ORD-011 có status bar "9:41" (mockup, không phải chụp máy thật); rà lại 59/82 ảnh Figma (`DOC-v1.0-04`) chưa từng được xem — tìm thấy bằng chứng thật cho nhiều nhóm (màn "Quà đã nhận" đầy đủ, chấm đỏ unread theo item ở Thông báo); đối chiếu `DOC-v1.0-02` (demo docx) tìm thấy nguyên văn §3.7/§3.9 trước đó bị bỏ sót khỏi REQ-ORD-011/REQ-USR-004. 3 nhóm còn lại không có bằng chứng tài liệu/ảnh nào: empty-state 3 màn + guard case Thông báo → giữ `Chờ BA bổ sung`; autocomplete địa chỉ (ORD-014) → user xác nhận là BA trả lời qua trao đổi → `BA xác nhận qua trao đổi (chat)`; scroll/phân trang Thông báo → user xác nhận là hành vi UI nền tảng, không cần xác nhận riêng → `UI nền tảng (scroll-load)`. Đã cập nhật cả 3 file TC-MASTER (byte-identical) + 4 fragment, và revert `C-ORD-06`/tách `C-NTF-03` trong `MEMORY.md` §6 (Resolved → Open/Partially Resolved) vì status "Resolved" trước đó không có bằng chứng cụ thể đi kèm. Chi tiết đầy đủ: `02_analyze-requirements/v1.0/MEMORY.md` header (bổ sung #13), `03_test-cases/v1.0/CHANGELOG.md`.
