# Create Test Plan — Mode REVIEW

> `/create-test-plan --review`

Đọc test plan → trình bày tổng quan cho user. KHÔNG sửa file.

Highlight: scope, quality gates, schedule, open risks.
Nếu phát hiện gaps → suggest `/create-test-plan --update`.
